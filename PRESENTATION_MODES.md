# PowerPoint制作方式の振り分け表

このファイルは**制作を始める前**に読む。1件につき主方式を1つ選び、必要な追加ルールだけ重ねる。画像の添付だけで「画像再生成方式」と決めない。会話で「前と同じ方法」が明確に今回の画像再生成版を指す場合は、その指定として扱う。

## 主方式

| 主方式 | 選ぶ条件 | 実体・基準 | 読む文書 |
| --- | --- | --- | --- |
| `consulting` | 提案・報告・経営会議向け、型カタログ指定 | 自由記述HTML→PDF、またはSlideSpec→**編集可能PPTX**。一般のスライド規約 | `SKILL.md`, `references/slide-rules.md` |
| `classroom-editable` | 通常の授業PPTX、教材画像から**内容を読み取って編集可能な文字で作る**指定 | 16:9、ネイティブ文字、通常は枠線なし、説明順のクリック表示 | `CLASSROOM.md`, `references/classroom-hard-gates-v3.md` ほか必読資料 |
| `source-image-click` | **原画像を画像生成で文字ごと再制作し、その新規画像を切り分けてクリック表示**と明示／前回のこの方式を明確に指す | 再生成した文字入り画像を個別のPowerPoint画像オブジェクトにする。原本忠実、画像内文字は直接編集不可 | `prompts/grammar-image-to-click-pptx.md`, `references/grammar-image-to-click-workflow.md` と授業共通の品質規則 |

画像が添付されただけで方式が不明なら、まず原本の読み取り・構成案などどちらの方式にも使える作業を進める。これまでの会話に明示された方式を尊重し、判断できない場合だけ「画像の文字も再生成するか、編集可能な文字にするか」を確認する。

## 追加ルール（主方式と混同しない）

| 条件 | 追加ルール | `source-image-click` での扱い |
| --- | --- | --- |
| 英語を学ばせる授業全般 | `EDUCATION.md`：目標、理解の順序、必要なら練習 | 原本を忠実に移す指定なら、**原本にない問題を機械チェックのために足さない**。原文台帳・各項目→スライド→クリックの対応表を作る。追加の練習を頼まれた場合は原本由来と追加分を区別する |
| イラスト・アイコンを**補助素材として**追加 | `ILLUSTRATION.md`：イラスト計画、重要な文字を絵に焼き込まない | 全ページを文字ごと再生成する制作自体には補助イラスト用の禁止を適用しない。別途補助イラストを追加する場合に適用する |
| 判断フローの制作を指定 | `references/classroom-flowchart-rules.md` | 元資料の分岐条件、矢印、判断順を保存し、画像としての表示順も点検する。ネイティブ編集可能なノードが必要なら方式変更が必要 |
| Google Slidesも納品 | `references/google-slides-output.md` | PPTXとGoogle Slidesの段階表示・動作検査を別に扱う。PPTXのon-clickをSlidesへ自動継承したとみなさない |

## `source-image-click` の制作宣言

作業メモの先頭に下記を記入してから設計する。値を後から変えるときは理由と影響する検査を記録する。

```yaml
mode: source-image-click
content_policy: source-only        # 原本にない教材を追加するなら明示
asset_policy: imagegen-then-crop   # 原画像の画素を切り取らない
text_policy: generated-raster      # 画像内文字は文字単位で編集不可
slide_count: adaptive              # 7～10枚は目安。内容で増減
slide_ratio: choose-for-teaching   # 前回は4:3。元画像が縦長でも比率を機械転写しない
reveal_policy: individual-on-click # 意味のある5クリック以上を目安
qa_profile: source-image-click
```

優先順位は **明示された今回の依頼 → この表の選択方式 → その方式に適用される既定規則**。画像再生成方式では「文字を焼き込まない」「16:9固定」「全文ネイティブ編集」「囲みを一律削除」「原本にない練習問題を必ず追加」は適用しない。読みやすさ、原文照合、英文の自然な改行、クリック時の非表示、教師用ノート、全ページ目視は適用する。通常の `classroom-editable` の基準は変更しない。

## 検査の分岐

- **`classroom-editable`**：`AGENTS.md`・`CLASSROOM.md`・`references/classroom-delivery-contract.md` の全機械チェックを通し、最終PNGを全枚確認する。
- **`source-image-click`**：同じ一般チェッカーも診断として実行し、16:9・ネイティブ文字タイトル等の**方式に合わない判定**を結果と理由付きで記録する。これらを「PASS」と書き換えない。方式固有の完了判定は下記すべてを実際に確認した場合に限る。

  1. 原文台帳の全項目が、最終スライドとクリック番号に対応し、原本にない内容が紛れ込んでいない。
  2. 最終画像が原本画素の拡大・切り出しではなく、新しく画像生成した資産から切り出され、各パーツの出典・座標が追跡できる。
  3. 各クリックは独立したPowerPoint画像とon-click効果に結びつき、初期表示で後出し情報や答えが見えず、自動送りしない。`scripts/check_click_reveal_xml.py` で名前・順序・Fadeを検査する。
  4. 内容スライドに説明ポイントとクリック順を含む教師用ノートがある。`undefined` 等の制作途中の文字列を残さない。
  5. 最終PPTXから全ページを書き出し、英文・和訳・図・切り抜き境界・可読性を1ページずつ原本と照合する。修正後は最終ファイルから再実行する。
  6. PowerPoint実再生まで確認したのか、PPTXの内部アニメーション設定のみ確認したのかを区別する。未検証項目・一般チェッカーの不適用を明示する。

画像内の文字サイズと綴りは既存のネイティブ文字チェッカーでは判定できない。画像の原文照合と最終PNGの目視をもって検査し、既存の一般QAを通過したとは言わない。条件を満たさないものを「画像再生成方式として検証済み」「完成版」と呼ばない。

## 既知の取り違え（前回の完了形8枚を例に）

- スライドは**4:3**、見出しも画像内文字。既存の `check_deck.py` / `check_classroom_hard_gates.py` / `check_classroom_deck.py` は標準の16:9やネイティブタイトルを要求するため、そのままではFAILする。これは画像の画質・英語の正しさの検査結果ではない。
- 画像再生成版は1枚**6・8クリック**を含む。通常の授業用の2～6クリックは推奨値であり、この明示指定には適用しない。意味のないクリック分割はしない。
- 通常の `check_education_storyboard.py` はretrieval/exit-ticketを要求する。原本忠実で確認問題が元画像にない場合、この検査を通すためだけに新問を捏造しない。
- 前回の8枚はPowerPoint実機でのクリック再生確認済みという意味ではない。検証していない動作は未確認と記す。
