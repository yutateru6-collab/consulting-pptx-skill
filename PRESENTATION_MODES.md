# PowerPoint制作方式の振り分け表

このファイルは**制作を始める前**に読む。1件につき主方式を1つ選び、必要な追加ルールだけ重ねる。最初に**入力画像があるか／入力内容だけか**を確認する。画像の添付だけで「画像再生成方式」と決めない。ユーザーによる編集可能性・画風・形式の明示指定を優先する。

## 主方式

| 主方式 | 選ぶ条件 | 実体・基準 | 読む文書 |
| --- | --- | --- | --- |
| `consulting` | 提案・報告・経営会議向け、型カタログ指定 | 自由記述HTML→PDF、またはSlideSpec→**編集可能PPTX**。一般のスライド規約 | `SKILL.md`, `references/slide-rules.md` |
| `classroom-editable` | 通常の授業PPTX、教材画像から**内容を読み取って編集可能な文字で作る**指定 | 16:9、ネイティブ文字、通常は枠線なし、説明順のクリック表示 | `CLASSROOM.md`, `references/classroom-hard-gates-v3.md` ほか必読資料 |
| `source-image-click` | **原画像を画像生成で文字ごと再制作し、その新規画像を切り分けてクリック表示**と明示／前回のこの方式を明確に指す | 再生成した文字入り画像を個別のPowerPoint画像オブジェクトにする。原本忠実、画像内文字は直接編集不可 | `prompts/grammar-image-to-click-pptx.md`, `references/grammar-image-to-click-workflow.md` と授業共通の品質規則 |
| `content-image-click` | **画像がなく、授業内容・原稿・主題が入力された**授業用PPTX。従来の画像パーツ方式の継続依頼を含む。ユーザーが編集可能な文字や別形式を明示した場合はそちらを優先 | 入力内容から「インフォグラフィック＋意味のあるフロー」の2K完成画像を先に作り、新しい画像の部分を等倍以下で切り出して個別クリック表示。文字は最終的に画像、直接編集不可 | `prompts/content-to-2k-image-click-pptx.md`, `references/content-to-2k-image-click-workflow.md` と授業共通の品質規則 |

**分岐**：入力画像があり、その画像の内容と画風を再現する指示なら `source-image-click`。入力画像がなく、授業内容から画像を起こすなら `content-image-click`。画像の有無に関係なく「文字・フローをPowerPoint上で編集したい」と明示されたら `classroom-editable`。非授業用の提案・報告は `consulting`。添付画像の用途が不明な場合は原本の読み取り・構成案など共通作業を進め、判断できない点だけ確認する。「前回と同じ」は、その会話で指した方式を確認して適用する。

## 追加ルール（主方式と混同しない）

| 条件 | 追加ルール | 画像パーツ方式での扱い |
| --- | --- | --- |
| 英語を学ばせる授業全般 | `EDUCATION.md`：目標、理解の順序、必要なら練習 | 原本／入力内容への忠実性を守る。**依頼のない問題を機械チェックのために足さない**。項目台帳と各項目→スライド→クリックの対応表を作る。追加練習を頼まれた場合は元の内容と追加分を区別する |
| イラスト・アイコンを**補助素材として**追加 | `ILLUSTRATION.md`：イラスト計画、重要な文字を絵に焼き込まない | 全ページを文字ごと再生成する制作自体には補助イラスト用の禁止を適用しない。別途補助イラストを追加する場合に適用する |
| 判断フローの制作を指定 | `references/classroom-flowchart-rules.md` | 問い、分岐条件、矢印、到達点を意味どおりに描き、画像としての表示順も点検する。画像パーツ方式では同書§8のネイティブ図形規則だけ例外とし、編集可能なノードが必要なら方式変更する。入力にない分岐を捏造しない |
| Google Slidesも納品 | `references/google-slides-output.md` | PPTXとGoogle Slidesの段階表示・動作検査を別に扱う。PPTXのon-clickをSlidesへ自動継承したとみなさない |

## `source-image-click` の制作宣言

作業メモの先頭に下記を記入してから設計する。値を後から変えるときは理由と影響する検査を記録する。

```yaml
mode: source-image-click
content_policy: source-only        # 原本にない教材を追加するなら明示
asset_policy: imagegen-then-crop   # 原画像の画素を切り取らない
text_policy: generated-raster      # 画像内文字は文字単位で編集不可
slide_count: adaptive              # 7～10枚は目安。内容で増減
slide_ratio: choose-for-teaching   # 前回は4:3。元画像が縦長でも比率を機械転写しない
reveal_policy: individual-on-click # 意味のある5クリック以上を目安
qa_profile: source-image-click
```

## `content-image-click` の制作宣言（入力画像なし）

```yaml
mode: content-image-click
input: supplied-content-or-topic       # 画像なし。事実の出所を項目台帳に記録
content_policy: supplied-and-verified  # 未提供の規則・例文・分岐は検証なしに追加しない
asset_policy: new-2k-master-then-crop  # 完成した新規2K画像を先に作る
text_policy: raster-in-final-pptx      # 正確な文字・分岐は決定的に作画して画像へ統合可
slide_count: adaptive
slide_ratio: choose-for-teaching
reveal_policy: individual-on-click
qa_profile: content-image-click
```

最初に**全体地図となる2K完成画像**を作り、続くページに必要なら同じ画風の2K詳細画像を新規制作する。最終画像は実寸で**長辺2048px以上**を満たし、元の生成・作画資産の画素数を単に引き伸ばして2Kと呼ばない。生成画像の絵柄・質感と、内容を正確に配置した文字・判断矢印を**完成した1枚のラスター画像**に統合してから切り出してよい。画像生成モデルの綴り・分岐線だけに文法の正確さを委ねない。

切り抜きPNGは元画像の画素を再標本化せずそのまま保存する。例えば横400pxの部分は、最終レンダリングで横400pxより大きく表示しない。必要な表示サイズに画素が足りなければ、**その項目専用の2K詳細画像を別途新規に作り**、そこから大きな領域を切り出す。全体図の小領域を拡大・アップスケールして済ませない。最終PPTXと実際の書き出し寸法を [`scripts/check_generated_image_parts.py`](scripts/check_generated_image_parts.py) に渡し、宣言値だけでなくPPTX上の表示画素が元素材を超えないことを確認する。

優先順位は **明示された今回の依頼 → この表の選択方式 → その方式に適用される既定規則**。画像パーツ方式では「文字を焼き込まない」「16:9固定」「全文ネイティブ編集」「囲みを一律削除」「機械ゲートのために練習問題を必ず追加」は適用しない。読みやすさ、入力内容との照合、英文の自然な改行、クリック時の非表示、教師用ノート、全ページ目視は適用する。通常の `classroom-editable` の基準は変更しない。

## 検査の分岐

- **`classroom-editable`**：`AGENTS.md`・`CLASSROOM.md`・`references/classroom-delivery-contract.md` の全機械チェックを通し、最終PNGを全枚確認する。
- **`source-image-click` / `content-image-click`**：一般チェッカーも診断として実行し、16:9・ネイティブ文字タイトル等の**方式に合わない判定**を結果と理由付きで記録する。これらを「PASS」と書き換えない。方式固有の完了判定は下記すべてを実際に確認した場合に限る。

  1. 元画像がある場合は原文台帳、ない場合は入力内容・検証済みの文法事実の項目台帳を作り、全項目を最終スライドとクリック番号に対応させる。未検証の文法規則・例文・判断枝を紛れ込ませない。
  2. 新たに作成した完成画像と各パーツの出典・切り出し座標を追跡できる。`content-image-click` は全体地図と各詳細画像の**実寸が長辺2048px以上**で、各パーツの画素が維持され、**最終PPTX上で切り抜きを一切拡大していない**ことをスクリプトと最終レンダリングで確かめる。画素数が不足する項目は詳細用2K画像を作り直す。
  3. 各クリックは独立したPowerPoint画像とon-click効果に結びつき、初期表示で後出し情報や答えが見えず、自動送りしない。`scripts/check_click_reveal_xml.py` で名前・順序・Fadeを検査する。
  4. 内容スライドに説明ポイントとクリック順を含む教師用ノートがある。`undefined` 等の制作途中の文字列を残さない。
  5. 最終PPTXから全ページを書き出し、英文・和訳・判断線・切り抜き境界・可読性を1ページずつ台帳と照合する。元画像があれば原本とも照合する。修正後は最終ファイルから再実行する。
  6. PowerPoint実再生まで確認したのか、PPTXの内部アニメーション設定のみ確認したのかを区別する。未検証項目・一般チェッカーの不適用を明示する。

画像内の文字サイズと綴りは既存のネイティブ文字チェッカーでは判定できない。入力内容との照合と最終PNGの目視をもって検査し、既存の一般QAを通過したとは言わない。条件を満たさないものを「画像パーツ方式として検証済み」「完成版」と呼ばない。

## 既知の取り違え（前回の完了形8枚を例に）

- スライドは**4:3**、見出しも画像内文字。既存の `check_deck.py` / `check_classroom_hard_gates.py` / `check_classroom_deck.py` は標準の16:9やネイティブタイトルを要求するため、そのままではFAILする。これは画像の画質・英語の正しさの検査結果ではない。
- 画像再生成版は1枚**6・8クリック**を含む。通常の授業用の2～6クリックは推奨値であり、この明示指定には適用しない。意味のないクリック分割はしない。
- 通常の `check_education_storyboard.py` はretrieval/exit-ticketを要求する。原本忠実で確認問題が元画像にない場合、この検査を通すためだけに新問を捏造しない。
- 前回の8枚はPowerPoint実機でのクリック再生確認済みという意味ではない。検証していない動作は未確認と記す。
