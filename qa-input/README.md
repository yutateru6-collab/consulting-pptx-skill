# qa-input

GitHub Actions の `Classroom PPTX QA` で確認したいPPTXを一時的に置くディレクトリ。

例：

```text
qa-input/
  lesson2-2.pptx
```

このディレクトリのPPTXをActionsが機械チェックし、PDF・PNG・コンタクトシートをArtifactに出力する。

授業内容や生徒情報を含むファイルをPublic repositoryへ置く場合は、公開してよい内容か必ず確認すること。
